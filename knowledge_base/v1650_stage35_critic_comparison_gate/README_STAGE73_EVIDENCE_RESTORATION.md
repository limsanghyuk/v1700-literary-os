# Stage73 Knowledge Base Evidence Restoration

This knowledge base subtree was added to make Stage72.3 reproducible as a full workspace package.
It contains the source evidence paths required by `pre_stage40_survival_gate`.

Some entries are compact evidence placeholders with lineage pointers because the Google Drive folder upload converted many files into Docs/folders and the active Stage72.3 package originally omitted the knowledge base dependency.

Policy:
- Active runtime lives under `gpt/active/v1700/literary_generator`.
- Historical evidence lives under `gpt/knowledge_base/v1650_stage35_critic_comparison_gate`.
- Stage73 packages both together so gates can be reproduced after download.
