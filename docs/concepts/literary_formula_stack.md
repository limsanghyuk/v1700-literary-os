# Literary Formula Stack

Stage73 restored the workspace baseline. Stage73.1 restores the literary formula stack that had been displaced by repository, GitNexus, and packaging work.

Core stack:

1. DRSE: Python-computed relation and weight scoring.
2. SceneGraphQueryEngine: scene-local GraphRAG focus extraction.
3. EmotionalMomentum4D: tension, sympathy, dread, catharsis vector.
4. Mise-en-scène Compiler: formula outputs to surface-safe sensory directives.
5. LiteraryRefinementLoop: critic issues to conservative revision.

Invariant: LLM/provider output is not trusted for structure. Python computes structure; Node2 renders only surface.
