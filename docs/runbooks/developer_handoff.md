# Developer Handoff

1. Read `README.md`.
2. Run `python -m pip install -e .`.
3. Run `python tools/run_runtime_smoke.py`.
4. Run `python tools/run_release_gate.py`.
5. Read `docs/stages/STAGE_INDEX.md` before changing architecture.
6. Add new live code under `src/v1700`, not the repo root.
