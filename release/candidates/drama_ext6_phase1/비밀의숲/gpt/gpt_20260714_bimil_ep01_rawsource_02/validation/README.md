# Validation

`gate_a_result.json` verifies contract integrity and deterministic recomputation. `gate_b_result.json` verifies direct HWP grounding and source-line evidence. `run_lock.json` freezes this candidate for cross-provider comparison. The full independent audit is included in the separately delivered ZIP; `independent_quality_summary.json` is its hub summary.
