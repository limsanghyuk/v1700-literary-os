# CT-07 결과 — 설계 깊이 응답 곡선: **후판 성립 (r=0.81)** + 예상 밖 서열 역전 (2026-08-07)

**문서 ID**: LOS-CT07-RESULT-V1.0 · 상위: 사전등록(40fd29f)+부록A(644a7d8, 생성 0건 상태 개정) · 봉인: 후판 sha `450fbb2b…`·70본 매핑 sha `e32b5694…` · 개봉 후 불변.

## 0. 한 줄
**설계도를 두껍게 하자 top-down이 성립했다(r 0.21→0.81).** 그리고 후판 설계도를 **직접** 주면 사람 씬카드보다 낫고(r=1.63), 정답표를 설계도로 주면 거의 만점이다(4.90/5) — **깊이가 병목이었고, 씬카드라는 중간 압축이 오히려 정보를 잃는다.**

## 1. 곡선 (70본 단일 블라인드 배치, 3인 일치 91.8%, 유효성 B−A=1.425 PASS)
| 팔 | 설계도 | 절대값 | **r** |
|---|---|---|---|
| A | 없음 | 0.000 | 0 |
| L1(gpt-5·박판, CT-06H 렌더 재채점) | 5필드 | 0.200 | 0.14 |
| L1-Gm(gemini·박판) | 5필드 | 0.100 | 0.07 |
| **L2-G(gemini·후판→카드 생성→렌더)** | **10필드** | **1.150** | **0.81 ✅ 성립** |
| B(사람 씬카드) | — | 1.425 | 1.00 |
| **L2-D(후판 직접 렌더, 카드 생략)** | 10필드 | **2.325** | **1.63** |
| **L3(정답표=설계도, 천장)** | 최대 | **4.900** | **3.44** |

## 2. 세 효과의 분해 (이 실험의 목적)
- **깊이 효과 = +0.74** (r_L2G 0.81 − r_L1Gm 0.07, 동일 생성기) — **지배적. CT-06H 불성립의 원인은 정보량이었다.** 개발자 가설("설계도가 얇으면 당연히 실패") 실증.
- **생성기 효과 = −0.07** (박판에서 gemini vs gpt-5) — 무시 가능. 얇으면 어느 모델도 못 한다.
- **생성 손실 = +0.83** (r_L2D 1.63 − r_L2G 0.81) — **후판→씬카드 압축 단계가 정보의 절반을 잃는다.** 씬카드 형식(intent_gist 1문장)이 병목.

## 3. 예상 밖 서열: L2-D(1.63) > B(1.00)
사람 씬카드보다 **후판 시퀀스 설계도를 통째로 주는 쪽이 낫다.** 함의: ①시퀀스층을 후판으로 재저작하면 **씬카드 생성 단계를 생략하는 2층 파이프가 실증 우위 후보**(거시기획 보충 v1.1 §3의 2′단 맞대결이 이미 이 방향) ②씬카드를 유지하려면 형식을 키워야 한다(1문장 gist → 기능 명제 목록).
**L3=4.90**: 설계-검사 동형 원칙의 천장 실증 — 검증 가능한 기능 명제 형식이면 렌더러는 거의 완전 수행. 상한은 열려 있다.

## 4. 판정
| 조건 | 값 | 판정 |
|---|---|---|
| r_L2G ≥ 0.70 | 0.807 | **성립** |
| 작품별(단독 통과 시 보류) | 건빵 0.81 / 한성 0.80 | **양작 통과 — 장르 의존 없음** |
| 민감도 s7 제외 | 0.692 | ⚠️ 경계(−0.115) — 성립 유지하되 **확정은 신규 2작 재현 세트(C3)로** |
| r>1 팔(L2D·L3) | C1 준용 | 성립+실효(드리프트 아닌 정보 우위로 해석 — B 재채점이 CT-06H와 동일 1.425로 안정) |

**한계**: 후판 N-컨트롤(불일치 후판) 미포함 — 재현 세트에서 추가. 후판 저작자가 원문 열람(사람 추출 상한의 표집 — "기획만으로 후판을 만들 수 있는가"는 다음 관문). 2작 스코프.

## 5. 다음 (판정이 지정하는 것)
1. **98작 후판 재분석 사양 확정 근거 획득** — cast/event/info_shift/plant_payoff/scene_notes 5필드. 단 확정 전 신규 2작 재현(C3)+후판 N-컨트롤.
2. **2층 파이프 정식 실험**(회차아크+후판 시퀀스 → 직접 렌더): L2-D 1.63의 확증판.
3. 씬카드 형식 개정 검토: gist 1문장 → 기능 명제 목록(L3 형식) — 설계-검사 동형의 제품화.
4. CT-05는 OpenAI 크레딧 충전 후(생성기 GPT-5 지정) — **개발자: 충전 요청**.
비용: gemini ~$3·렌더/채점 서브에이전트. 산출물 outputs/ct07/ 전량.
