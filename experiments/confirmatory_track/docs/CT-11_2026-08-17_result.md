# CT-11 결과 — 회차 구성 자율 설계: **불성립** (균등분할을 못 이김) · 단 교차검색 기여는 최대 (2026-08-17)

**문서 ID**: LOS-CT11-RESULT-V1.0 · 상위: 사전등록(982ac5a, 생성 전 고정) · 패킷 봉인 sha `1dcd4c7b…` · 생성기 gemini-2.5-pro(파일 접근 없음), 채점 전량 결정론.
**집행 편차 1건(공시)**: C팔 초기 호출이 `maxOutputTokens=3000`에서 응답 잘림(추론 토큰 소모) → **9000으로 상향 후 재호출**. 프롬프트·표본·임계 불변, 판정과 무관.

## 1. 유효성
생성 형식 준수 **36/36**(경계 단조·범위 내·중복 없음). 결측 0.

## 2. 결과 (9회차 평균, 내부 경계 기준)
| 팔 | 입력 | F1(정확) | **F1(±1)** | \|Δ시퀀스수\| | IoU |
|---|---|---|---|---|---|
| **U 균등분할(기준선)** | 정보 0 | 0.239 | **0.465** | **1.22** | **0.650** |
| A | 씬 목록만 | 0.220 | 0.374 | 3.22 | 0.505 |
| B | +회차 설계층 | 0.232 | 0.401 | 3.22 | 0.535 |
| **C** | +교차작품 사례 | **0.266** | **0.471** | 1.67 | 0.594 |
| N | +불일치 설계층 | 0.197 | 0.384 | 3.22 | 0.507 |

**주판정: max(B,C) − U = +0.007 (임계 +0.10) → 불성립.**
부수: 층 기여 B−A **+0.028**(임계 +0.05 미달) · **교차검색 기여 C−B +0.070(임계 +0.05 충족)** · 내용 귀속 B−N **+0.018**(미달).
작품별(F1±1): 모래시계 U0.30/C0.64 · 뉴하트 U0.43/B0.61 · **도깨비 U0.67/C0.23** — 도깨비에서 균등분할이 압도(원작이 7씬 등간격에 가까움), 나머지 2작은 생성이 우세. **장르 의존이 지배적이며 한 작품이 판정을 뒤집는다**(사전등록 민감도 조항대로 ★보류 성격).

## 3. 해석 — 세 가지가 분리되어 보인다
1. **시퀀스 개수 판단이 가장 큰 실패 원인**: A·B·N이 |Δk| 3.22로 사람보다 크게 적게/많게 나눈다(대개 과소분할). U는 1.22, C는 1.67 — **교차작품 사례가 개수 감각을 절반 이상 교정**했다. 즉 "몇 개로 나누는가"는 **다른 작품의 분포를 봐야 알 수 있는 지식**이며, 그 회차의 설계층만으로는 알 수 없다.
2. **회차 설계층(B)의 기여는 약하다(+0.028)**. 그리고 **내용 귀속도 약하다(B−N +0.018)** — 불일치 설계를 줘도 비슷하다는 것은, 현재 episodearc가 경계 판단에 실질 정보를 거의 주지 못한다는 뜻이다(막 구조의 seq_span을 누출 차단으로 제거하면 남는 것은 서술뿐이다).
3. **균등분할이 이렇게 강하다는 사실 자체가 발견**이다. 코퍼스 중앙값 7.2씬으로 등분하면 F1±1 0.465 — 어떤 생성 조건도 유의미하게 넘지 못했다. 향후 이 트랙의 모든 주장은 **반드시 이 기준선과 비교**되어야 한다(신규 결함으로 등재: 균등분할 기준선 의무화).

## 4. 이 결과가 부정하지 않는 것
CT-07/CT-08의 결론(후판 설계가 **씬 생성**을 조향한다)은 영향받지 않는다. 본 실험은 **회차를 시퀀스로 분할하는 판단** 하나만 쟀고, 그것이 현행 회차층으로는 부족함을 보였다. 또한 경계 일치는 형식 지표이므로 "사람과 다르게 끊었다=나쁘다"가 아니다(사전등록 §5).

## 5. 다음 (제안)
1. **C의 성공 요인을 확대**: 교차검색을 사례 5건 → 20건 + **기능 유사도 검색**(episode_function 임베딩)으로 강화하고, "개수 예측"과 "위치 예측"을 분리한 2단 설계(먼저 k를 추정→그다음 경계)로 재시험. 개수만 맞춰도 IoU가 크게 오른다.
2. **회차층 스키마 보강**: 현행 episodearc는 경계 판단에 기여하지 못했다. 막별 **씬 배분 근거**(예: 막 전환이 어떤 사건에서 일어나는가)를 서술이 아니라 **판정 가능한 형태**로 추가해야 한다 — CT-07이 시퀀스층에서 얻은 교훈("얇으면 낳지 못한다")의 회차층 판이다.
3. 장르 층화: 도깨비형(등간격)과 모래시계형(불균등)을 분리해 표본을 늘린다.
