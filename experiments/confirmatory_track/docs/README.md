# 확증 트랙 (Confirmatory Track)

**개발 트랙과 분리된 별도 진행 방향.** 개발자 지시(2026-08-05)로 신설.

이 트랙은 코드를 만들지 않는다. 만드는 것은 **판정**이다 — 층을 계속 저작할 것인가,
동결할 것인가, 버릴 것인가. 개발 트랙이 "어떻게 하면 더 잘 되는가"를 빠르게 반복한다면,
이 트랙은 "되는가, 안 되는가"를 느리게 확정한다.

## 읽는 순서

1. **`TRACK_CHARTER.md`** — 왜 별도 트랙인가, 규칙, 명칭 규약, **미측정 목록**
2. **`EXPERIMENT_LEDGER.md`** — 판정 이력 단일 권위표 + 기준선 수치 + 미완료 큐 (v1.2)
3. **`METHOD.md`** — 재현 지침 (사전등록 서식·네거티브 컨트롤·블라인드·통계)
4. **`AUDIT_2026-08-05_self_reproduction.md`** — ★보고 수치의 자기 감사 (93항목 대조)
5. **`CT-03_2026-08-05_craft_parity_result.md`** — 최종 판정의 자, **FAIL**
6. **`CT-06_2026-08-05_function_key_status.md`** — 정답표 v1.2 + **D-37**
7. **`SCHEMA_FINDING_2026-08-05_character_layer_gap.md`** — 인물층↛설계층 격차
8. **`CT-07R_2026-08-07_result.md`** — 후판 재현 관문 **PASS(강한 재현 아님)** + 범주 분해
9. **`CT-08A_2026-08-08_result.md`** — 후판 5필드 절제 파일럿 **무효(INVALID)**
10. ★**`../../standards/CT_DEFECT_REGISTER_2026-08-08.md`** — **D-53·D-54 계측기 결함.
    CT-07R 의 PASS 지위가 여기서 강등된다**
11. **`../../handoff/2026-08-08_HANDOFF_CT08A_and_schema_consumption.md`** — 집 이어작업용 인계문 (최신)
12. `../../handoff/2026-08-07_HANDOFF_CT07R_thick_replication.md` — 직전 인계문

## 현재 상태 (2026-08-08 기준)

| ID | 실험 | 판정 |
|---|---|---|
| CT-01 | 씬층 사실충실 | **PASS** (Δ +1.236 / +1.173, p=0.0039) |
| CT-02 | 시퀀스층 거시충실 | **FAIL** (Δ +0.250, 임계 0.5 미달) → `authored_seq/` 동결 |
| **CT-03** | **작법 필적 (인간 대비)** | **FAIL** — BA **0.979** CI[0.947, 1.000]. 84문항 중 82 적중 |
| CT-05 | 회차층 조향 (이중 모델 3팔) | 사전등록 v1.1, 미집행 |
| CT-06 | top-down 자기생성 관문 | 사전등록 v1.0 + **정답표 v1.2 완료(49요소, fail 0)**. 집행 대기 |
| CT-06H | top-down 관문 집행판 (4팔) | **불성립** — r=0.211. 기각된 것은 top-down 구조가 아니라 **현행 5필드 인코딩** |
| CT-07 | 설계 깊이 응답 곡선 (7팔 70본) | **성립** — r_L2G=0.807, 후판 직접 렌더 1.63 > 사람 씬카드 1.00 |
| **CT-07R** | **후판 재현 관문 (2작 10앵커 4팔 40본)** | **PASS — 계측기 미검증 상태의 PASS** (D-54로 강등). r_T **0.817** · D_N **+2.533**. ★범주 분해: 씬내부 0.567 / 배치관계 1.386 |
| **CT-08A** | **후판 5필드 절제 파일럿 (5팔 24앵커 120본)** | **무효 (INVALID)** — Δ_true +1.479 p=3e-14 였으나 채점자 일치도 3쌍 전부 <0.90 → 사전등록 §8-2 발동 |
| CTX-01 | 인물 집중도 | 탐색적, 판정 미사용 |
| CTX-02 | 해석 편차 바닥값 | 탐색적, 미집행 |

## 이 트랙이 지금까지 바꾼 것

- 씬층과 시퀀스층 사이에서 **자원 배분이 처음으로 데이터로 갈렸다.**
- `authored_seq/` 신규 저작 중단(동결, 삭제 아님). 유예 조건 1개 명시 = CT-06.
- 저작 자원 1순위를 신규작 편입에서 **스키마 갭**(plant/payoff/information_reveal)으로 이동.
- 사전등록이 없었다면 CT-02의 Δ=+0.250과 "8범주 중 2범주 우세"를 통과로 쓸 수 있었다.
- ★**CT-03 이 실패의 성질을 뒤집었다.** 작법 점수는 생성물이 **더 높은데도**(격차 −0.108) 거의 전부 들킨다. 채점자 사유의 **79.2%가 "너무 정돈돼 있다"**. 못 써서 들키는 것이 아니라 **질서 때문에** 들킨다. 개선 방향이 "더 잘 쓰기"에서 **"불규칙성 재현"**으로 바뀐다.
- ★**CT-07R이 98작 후판 저작을 승인했으나, 승인의 정체가 예상과 다르다.** 전체 r_T 0.817은
  씬내부 0.567(사람 씬카드가 여전히 우위)과 배치관계 1.386(후판이 씬카드를 넘어섬)이 섞인 값이다.
  후판은 씬카드를 **대체**하지 않는다 — 씬카드가 구조적으로 다룰 수 없는 **배치관계 층**을 채운다.
  98작 발주 지시문의 무게중심을 씬 묘사가 아니라 **앞뒤 씬과의 관계**에 두어야 한다는 뜻이다.
- ★**효과 크기는 재현되지 않았다** — CT-07 1.63 대 CT-07R 0.817. 부호와 존재는 재현, 크기는 절반.
  단일 배치 결과를 발주 규모 산정의 근거로 쓰지 않는다.
- ★**LLM 검증·채점의 신뢰 한계가 두 실험에서 같은 형태로 나왔다**(D-34 / D-37). 채점자는 재려는 것을 못 재면 잴 수 있는 것을 잰다. **LLM 통과를 품질 증거로 쓰지 않는다**가 이 트랙의 규칙이 됐다.

## 원자료와 재현

원자료 로컬 경로: `C:\claude\CT07R_run_20260807\` · `C:\claude\CT03_run_20260805\` · `C:\claude\CT06_run_20260805\` · `C:\claude\CT_AUDIT_20260805\` · `C:\claude\G1_run_20260804\` · `C:\claude\G1SEQ_run_20260804\` · `C:\claude\G2CAST_run_20260805\`

```
python docs/tracks/confirmatory/artifacts/audit_recompute.py --root C:\claude
```
원자료만 읽어 보고서의 수치를 다시 계산하고 대조한다. 기대 출력 = `artifacts/AUDIT_OUTPUT.txt` (93항목 / OK 91 / 불일치 2 — 불일치 2건은 감사 문서 §3 에 이미 정정 반영).

## ★허브 경계

각 실행 디렉터리의 `orig/` 및 `items_raw.json`·`packets/`·`items/` 는 **대본 원문 전문을 담으므로 허브 커밋 금지.**
허브에 올린 정답표·문항표는 전부 **`*.hubsafe.json` 안전판**이다 — 직접인용을 앞 8자로 절단하고, 원문 행 사전과 완전일치 대조를 통과시킨 사본이다(`artifacts/make_hubsafe.py`, `artifacts/make_hubsafe_ct03.py`, 게이트 출력 `artifacts/HUBSAFE_OUTPUT.txt`).
**채점 집행에는 안전판을 쓰지 말 것.** 집행 정본은 로컬이며 sha256 이 각 안전판의 `source_sha256` 에 기록돼 있다.

### CT-07R 재계산 (원자료 없이 허브만으로 성립)

```
python docs/tracks/confirmatory/artifacts/ct07r/CT07R_analyze.py --run docs/tracks/confirmatory/artifacts/ct07r
python docs/tracks/confirmatory/artifacts/ct07r/CT07R_posthoc_cat5.py --run docs/tracks/confirmatory/artifacts/ct07r
```
채점 결과(`scores/scorer*.json`) + `BLIND_MAP.json` + `element_labels.json` 셋만 읽어
보고서 §2~§6의 모든 수치를 다시 계산한다. **정답표 원문·렌더 원문 없이 닫힌다** — 채점이 이미
끝났으므로 판정 산술에 원문이 필요 없다. 기대 판정 `PASS`, `r_T=0.817`, 일치도 95.3%.

### CT-07R 허브 경계 처리 (신규 선례)

렌더 40본은 **모델 생성물이지 대본 원문이 아니므로** 절단하지 않고 원본 그대로 올렸다.
대신 정량 축자 게이트를 통과시켰다 — 원문 행 사전 30,579행(2작)과 대조하여
**표제(프롬프트가 공급한 씬 헤딩) 제외 최장 일치 15자, 차단 기준 16자 이상 0건**.
게이트 스크립트 `artifacts/ct07r/CT07R_make_hubsafe.py`, 전체 일치 목록
`artifacts/ct07r/hubsafe/CT07R_VERBATIM_GATE.json`. **일치 건수를 숨기지 않고 전량 공개**하므로
판정을 신뢰하지 않고 직접 대조할 수 있다.
정답표는 기존 선례대로 인용 8자 절단 안전판만 올렸다(`hubsafe/CT07R_KEYS.hubsafe.json`, 38건 중 21건 절단).
**집행 정본 정답표는 허브에 없다** — 로컬 `C:\claude\CT07R_run_20260807\keys\` 이며 sha256만 봉인돼 있다.

---

## ★2026-08-08 갱신 — 계측기가 먼저다

CT-08A 를 집행했고 **무효**로 기록했다. 효과는 크게 나왔지만(Δ_true +1.479, McNemar p=3.21e-14)
사전등록이 집행 **전에** 정해 둔 채점자 일치도 0.90 을 세 쌍 전부 넘지 못했다
(0.8783 / 0.8525 / 0.8942). 결과를 본 뒤 기준을 낮추지 않았다.

무효를 파고들어 계측기 결함 두 건을 등록했다 → `../../standards/CT_DEFECT_REGISTER_2026-08-08.md`

- **D-53** 기능충실 채점의 재현성 부족. 불일치는 팔이 아니라 **경계 요소**의 함수
  (TN 0.967 → T5 0.817).
- **D-54** 이진 일치도의 **기저율 팽창**. 둘 다 0을 준 요소가 자동 일치로 세어진다.
  비자명 요소(최소 1인이 1)로 제한하면 CT-08A 0.8750 → **0.7660**,
  **CT-07R 0.9533 → 0.8621**.

D-54 의 파장이 크다. CT-07R 이 근거로 인용한 "3인 일치 95.3%"는 기저율이 끌어올린 값이고,
실제로 판정이 갈리는 요소에서는 **CT-07R 도 0.90 을 넘지 못한다.**
판정을 철회하지는 않는다(CT-07R 은 자기 사전등록을 위반하지 않았다). 다만 지위를
**"계측기가 검증되지 않은 상태의 PASS"** 로 낮추고, **이 PASS 를 근거로 후판 98작
전량 저작을 착수하지 않는다.**

**순서는 고정이다.** ①채점 규칙 개정(관측 전 정당화) → ②비자명 일치도 조항 신설 →
③CT-01·CT-06H·CT-07 소급 재산출 → ④CT-08A 재채점 → ⑤그 다음에야 절제와 전량 저작.
계측기를 고치기 전의 대결 실험은 또 무효가 될 수 있다.
