# CT-06-H 결과 — top-down 관문: **불성립 (r = 0.21)** (2026-08-07, 집 세션 집행)

**문서 ID**: LOS-CT06H-RESULT-V1.0 · 상위: CT-06H 사전등록(da1bc26, 데이터 생성 전 고정) · 개발자 직접 지시 집행.
**봉인**: 패킷 18 sha `24d28d6a…`(사전등록 재구성 일치) · 블라인드 매핑 sha `64b52c3d…` · 채점 후 개봉, 되돌리지 않음.

## 0. 한 줄
**시퀀스 설계도만 받은 GPT-5가 생성한 씬카드는, 사람 씬카드가 옮기는 기능 정보의 21%만 옮긴다.** 사전등록 임계 r≤0.30 → **불성립**. C4 행동 4항 발동.

## 1. 집행 요약 (사전등록 대로)
생성기 gpt-5(컨텍스트 분리 18호출, 130매, 형식 오류 0) → 렌더 Sonnet 조건격리 4에이전트 40본(600~800자, 4팔 분량 균형) → 채점 Sonnet 블라인드 3인, 49요소×40본=196판정×3 → 개봉.
채점 신뢰성: **3인 완전일치 95.9%**(188/196) — 판정이 채점자 개인차에 좌우되지 않음.

## 2. 결과 (기능충실 0–5, 3인 중앙값의 팔 평균)
| 팔 | 값 | 씬별 분포 |
|---|---|---|
| A (설계 없음) | **0.000** | 10/10 전부 0 |
| B (사람 씬카드) | **1.425** | 8/10 양수, 최대 4.0 |
| **B″ (GPT-5 생성 씬카드)** | **0.300** | 2/10만 양수(건빵 s15=2.0, s1=1.0) |
| N″ (불일치 설계 생성 카드) | **0.000** | 10/10 전부 0 |

**r = (B″−A)/(B−A) = 0.211** → 임계 r≤0.30 **불성립**. B″−N″=0.30(부분성립 하한 0.5에도 미달).
민감도: s7 제외 r=0.231(판정 불변) · 한성 S05(s42) B″=0(단독 기여 없음) · **작품별: 건빵 r=0.32, 한성 r=0.00**(양쪽 다 성립권 밖 — 장르 의존 아님, 전반 미달).

## 3. 부수 확인 (판정 외 소득)
- **A=0.000, N″=0.000** — CT-01의 A=0/96·N≈A 패턴 완전 재현: 암기 누수 없음, 틀린 설계는 아무것도 못 옮김, **사람 씬카드(B)만이 신호를 옮긴다**가 독립 배치에서 재확인. 층의 가치(CT-01 PASS)는 흔들리지 않는다.
- 범주 분해: B는 goal 0.50·link 0.33·나머지 0.20대에서 고루 적중. B″ 적중 3건은 goal·link·character 각 1— **conflict·info는 0**. 생성 카드는 "무엇을 하려는가"는 가끔 옮기지만 "무엇이 저항하고 누가 무엇을 알게 되는가"는 전혀 옮기지 못했다.
- 원인 소견(탐색적): B″ 카드 자체는 형식 완결(130/130). 문제는 내용 — 시퀀스 5필드는 씬 하나하나가 해야 할 일을 특정하기엔 정보가 너무 성기다. CT-02("시퀀스층은 씬층 위에서 잉여")와 합치면 결론이 수렴한다: **현행 5필드 인코딩의 시퀀스층은 씬층을 낳지도(CT-06H), 씬층을 보태지도(CT-02) 못한다.**

## 4. 판정에 따른 행동 (C4, 사전등록 그대로 발동)
1. authored_seq 신규 저작 동결 → **확정 DROP 심의 상정**(부활 조건 소진). 개발자 결재 대기.
2. 대안 사전등록 예약: **2층 파이프**(회차 아크→씬카드 직행, 동일 앵커) — 시퀀스층 생략 시 r이 오히려 오르는지.
3. 코퍼스 시퀀스층 용도 재분류: 생성 원천 → **색인·검증·수정전파 축**.
4. 거시기획 재정박 S3→S2(경계 판별자)는 **영향 없음, 계속** — 경계 판정과 카드 생성은 별개 질문.

## 5. 한계 (§7)
편차 6건(사전등록 공시): 특히 **CT-01 절대값과 비교 금지**(B=1.425는 이 배치·이 정답표·보수 채점 기준의 값), 정답표는 hubsafe판(인용 절단 — 다만 A·N″의 0.000과 3인 95.9% 일치가 채점 가능성을 실증), 2작 스코프 한정. 회사 봉인판(GPT-5 생성 예정) 집행 시 본 결과와 상호 재현 대조 가능 — r의 방향이 재현되는지가 관전점.
**이 실험이 재지 않은 것**: 작법·효용·인간 필적(CT-03 FAIL 불변) · 회차→시퀀스 칸(미측정, 거시기획 트랙 몫).

## 6. 산출물 (세션 outputs/ct06h/)
packets.json(봉인)·gen/ 130매·renders_{A,B,BP,NP}.json·blind_items·SEALED_MAP·scores_J0~2·RESULT_scores.json. 비용 ~$4(GPT-5 18호출)+서브에이전트.
