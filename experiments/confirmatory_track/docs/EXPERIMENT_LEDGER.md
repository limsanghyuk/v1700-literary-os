# 확증 트랙 — 실험 원장

**문서 ID**: LOS-CT-LEDGER-V1.4 · 최종 갱신 2026-08-08 (**CT-08A 집행·무효** · **D-53/D-54 등록 → CT-07R PASS 지위 강등**)

이 표가 트랙의 단일 권위다. 개별 보고서와 수치가 다르면 **개별 보고서가 옳고 이 표를 고친다.**

---

## 1. 판정 이력

| ID | 실험 | 사전등록 | 집행 | 1차 종점 | 임계 | 실측 | **판정** |
|---|---|---|---|---|---|---|---|
| **CT-01** | 씬층 사실충실 스크리닝 | 08-04 13:58 | 08-04 14:12 | 사실충실 0–5 | Δ(B−A)≥0.5 **및** Δ(B−N)≥0.5 | +1.236 / +1.173 | **PASS** |
| **CT-02** | 시퀀스층 거시충실 | 08-04 14:24 | 08-04 15:16 | 거시충실 0–5 | Δ(B′−A′)≥0.5 | **+0.250** | **FAIL** |
| **CT-03** | 작법 필적 (인간 대비) | 08-05 15:55 | **08-05 12:39** | 균형정확도 + 작법품질 격차 | CI상한<0.65 **및** 격차≤0.5 | **BA 0.979** CI[0.947,1.000] · 격차 **−0.108** | **FAIL** |
| **CTX-01** | 인물 집중도 (탐색적) | 해당없음 | 08-05 | — | — | 아래 §4 | 판정 미사용 |
| **CT-06** | top-down 관문 — 시퀀스층→씬층 자기생성(B″/N″) | **08-05 v1.0** | 미집행 | 기능충실 (5범주×10씬 = **49요소** 정답표 v1.2) | **r=(B″−A)/(B−A) ≥0.70 AND B″−N″≥1.0** · r≤0.30 불성립 | — | **정답표 완료(fail 0)·집 회신=조건부 집행(C1~C4, 08-07)·단계2 착수 가능** |
| **CT-06H** | top-down 관문 집행판(집, 4팔 단일배치) | 08-07 prereg | **08-07 집행** | 기능충실 49요소 | r≥0.70 / r≤0.30 | **r=0.211** (B 1.425·B″ 0.300·A=N″=0.000) | **불성립 → C4 발동 (D-54: 비자명 일치 0.778, 08-09 산출)** |
| **CT-07** | 설계 깊이 응답 곡선(7팔 70본 단일 배치) | 08-07 prereg+부록A | **08-07 집행** | 기능충실 49요소 | r_L2G≥0.70 | **r_L2G=0.807 성립**·L2D=1.63>B·L3=4.90·깊이효과+0.74 지배 | **성립 — 계측기 미검증 상태(D-54: 비자명 일치 0.838, 08-09 산출)** |
| **CT-07R** | 후판 재현 관문(2작 10앵커 4팔 40본) | **08-07 prereg + AMD-01 + AMD-02** | **08-07 집행** | 기능충실 5요소×10앵커 = 50요소 | B−A≥0.5 · r_T≥0.70 · 작품별 r_T>0.30 · TN<T | **r_T=0.817 · D_N=+2.533 · B−A=3.100 · 작품별 0.755/0.886 · 3인 일치 95.3%** | **PASS — 계측기 미검증 상태의 PASS** (D-54, §7 참조) |
| **CT-08A** | 후판 5필드 절제 파일럿(5팔 24앵커 120본) | **08-08 prereg** | **08-08 집행** | 기능충실 240요소 | Δ_true≥0.5 · **§8-2 무효조건: 쌍별 일치도≥0.90** | Δ_true **+1.479** CI[+0.958,+2.000] p=3.21e-14 · 쌍별 일치도 **0.878/0.853/0.894 (3쌍 전부 미달)** | **무효 (INVALID)** — 수치는 기록만 하고 채택하지 않는다 |
| **CT-08A-R** | v2 재채점(처치 불변·계측 교체) | 08-09 prereg | **08-09 집행** | 기능충실 240요소(v2 서브 589) | Δ_true≥0.50 · 승계 무효조건 | **Δ_true +1.104 CI[+0.69,+1.52]·TN 0.000·B−T5 −0.08** | **불선언 — 장편 괴리 0.271≥0.25 발동(상방 괴리). CT-08B 확인 런으로 종결 예약** |
| **CT-08B** | 확인 런(신규 블라인드·신규 채점) | 08-11 prereg | **08-11 집행** | 동일 | 게이트 평균0.80+최저0.75 | 일치 0.779/0.754·**Δ_true +1.208 CI[+0.83,+1.60](기록)**·TN 0.000 | **불선언(게이트 평균 −0.021 미달). 3런 관측 일관·판정 비일관 → 계측기 병목 확정, 안A/B 개발자 결정 대기** |
| **CT-10** | 전방생성 확증(3팔 B/C/N × 신규 3작 × 3회차 롤아웃) | **계획 08-14(승인 대기)** | 미집행 | 홀드아웃 호환(v2 요소표) | C−N≥10 AND C−B≥3 · 감쇠≥−15 · 하드게이트 | — | **계획 — 선행: CT-08B 종결·정답표 저작** |
| **CTX-02** | 해석 편차 바닥값 (탐색적) | 해당없음 | 미집행 | — | — | — | 판정 미사용 |
| **CT-05** | 회차층 설계 조향, 이중 모델(Claude+GPT-5), 3팔(E/F/N) | 08-05 v1.1 | 미집행 | turn_match·intent_proximity (결정론 채점기 sha=6a35ca49d1a6a057) | 상대+50% AND E−N 분리 AND 민감도, 모델별 | — | **미집행** |

사전등록 시각이 집행 시각보다 앞선다는 것이 이 표의 존재 이유다.

## 2. CT-01 — 씬층, PASS

홀드아웃 10씬(건빵선생과별사탕_01 s1·7·10·15·19 / 한성별곡_01 s1·14·27·42·57) × 3조건 = 30본.
정답표 96 사실요소.

| 지표 | A 맨프롬프트 | **B SeqCard** | N 불일치대조 |
|---|---:|---:|---:|
| 사실충실 0–5 (1차) | 0.00 | **1.24** | 0.06 |
| 기능충실 0–5 | 0.70 | **3.20** | 0.60 |
| 환각 (합) | 35 | **24** | 40 |
| 인물 재현율 | 0.04 | **0.83** | 0.34 |

Δ(B−A) = **+1.236** CI[+0.697, +1.781] 양수 8/10 p=0.0039
Δ(B−N) = **+1.173** CI[+0.634, +1.747] 양수 8/10 p=0.0039

**의미의 한계선.** ①절대 수준 1.24/5 = **24.8%**. 원본 사실의 3/4은 설계층을 줘도 복원 안 됨.
②장르 격차 3.2배(건빵 1.88 / 한성 0.59) — 사실 밀도 높은 사극에서 스키마가 거의 못 옮김.
③A조건은 96요소 중 **0개** 재현 — 헤딩과 로그라인만으로는 같은 씬이 재현되지 않는다.
④**N이 A보다 환각을 늘렸다(40 vs 35)** — 설계층은 중립 보조가 아니라 방향을 가진 조향 장치.

**이 실험의 진짜 소득은 N조건이다.** 우위의 정체가 "구조화된 텍스트를 더 준 것"이 아니라
"그 씬에 대해 우리가 저작한 내용"임이 처음 분리됐다. 과거 실험(2026-07-03 수직슬라이스,
07-04 축C 파일럿)에는 이 대조군이 없어서 양성이 나와도 이 구분을 못 했다.

## 3. CT-02 — 시퀀스층, FAIL

**기저선이 강하다는 점이 CT-01과 다르다.** A′는 시퀀스 소속 전 씬의 SceneBlueprint를 전부 받는다.
질문: *씬층을 이미 다 준 상태에서 시퀀스층은 무엇을 더 하는가.*
홀드아웃 12시퀀스 × 3조건 = 36본. 고정 8범주 120요소.

| 지표 | A′ 씬층만 | **B′ 씬층+시퀀스층** | N′ 불일치 |
|---|---:|---:|---:|
| 거시충실 0–5 (1차) | 3.667 | **3.917** | 3.750 |
| 환각 (합) | 75 | 78 | 73 |
| 분량 L1 | 0.151 | 0.160 | 0.150 |
| 씬수 준수 | 11/12 | 12/12 | 11/12 |

Δ(B′−A′) = **+0.250** CI[−0.125, +0.667] 양수4 음수3 동점5 p=1.00 → **임계 0.5 미달, FAIL**
Δ(B′−N′) = +0.167 CI[−0.167, +0.625] p=1.00

**민감도.** 유일한 큰 양수 한성 S05(+2.00)를 빼면 나머지 11개 Δ = **+0.09**. 신호 전체가 1시퀀스 산물.
**작품별.** 건빵 A′ 4.33 / B′ 4.50 / **N′ 4.58(최고)** · 한성 A′ 3.00 / B′ 3.33 / N′ 2.92.
**범주별.** B′가 앞선 것은 전환점내용(+0.17)·인과연결(+0.14) 둘뿐.
**시퀀스층이 담당한다던 goal·obstacle·value_shift 3항목에서 전부 못 이겼다.**

### 기전 3가지

1. **씬층이 이미 거시구조를 함축한다.** A′ 단독으로 73% 재현.
2. **충돌 시 렌더러는 씬층을 택한다.** N′ 렌더러들이 불일치를 알아채고 씬층 우선을 **자가보고**했다.
   그래서 틀린 시퀀스층이 환각을 **늘리지 못했다**(73 vs 75). CT-01과 정반대다.
   조향력 자체가 없다는 뜻이며, 이것이 FAIL의 진짜 근거다.
3. **못 넘어간 것은 시퀀스층이 아니라 스키마 갭.** 한성 S06·S07의 목표/장애/가치종료가
   조건 무관 6본 전부 0. `plant / payoff / information_reveal` 부재가 다시 확인됐다.

### 결함 공시

`turn_position` 오프바이원(건빵 B군 9본, 상대위치는 정답) — **사후 규칙 변경 거부**, 3조건 대칭.
`한성_S07__D3` 5,026B 절단 — 개봉 결과 **N′ 조건**이었으므로 B′ 부풀림 아님.
n=12 · 채점자 LLM · 봉인 예비 6시퀀스 미열람(FAIL이므로 복제 미실시).

### 결정

**"쓸모없다"가 아니라 "씬층 위에서는 쓸모없다"가 측정된 것이다.** 실제 파이프라인에서
시퀀스층의 소비자는 렌더러가 아니라 **씬층 생성기**이고, 그 방향은 측정하지 않았다.

1. `authored_seq/` **신규 저작·신규작 편입 즉시 중단. 삭제 아닌 동결.**
2. **유예 조건은 단 하나** — 시퀀스층만 주고 **씬층을 생성**시켜 사람 저작 씬층과 비교
   (네거티브 컨트롤 필수). 여기서도 FAIL이면 최종 DROP.
3. 저작 자원 전량을 **스키마 갭**(plant/payoff/information_reveal)으로. 신규작 편입보다 우선.
4. 자기생성 조건은 **씬층에서 먼저**. 씬층만이 증분 입증된 층이다.

## 3.5 CT-03 — 작법 필적, **FAIL** (집행 2026-08-05)

**최종 판정의 자로 지정된 실험이다**(문서 말미 "성공의 최종 정의" §3). 질문: *블라인드에서 인간 작가의 씬과 우리 렌더가 구별되지 않는가.*

문항 72 + 앵커 12 = **84**. 3블록 라틴방격. 채점자 3인(J0·J1·J2, 전부 LLM). 표기 정규화 후 봉인(`CT03_SEALED_MAP.json`).

| 지표 | 값 | 임계 | 판정 |
|---|---:|---|---|
| **균형정확도 BA** | **0.979** CI[0.947, 1.000] | CI **상한** < 0.65 | **FAIL** (하한 0.947 ≥ 0.65) |
| 단순 정확도 | 0.972 | — | — |
| 작법 품질 H(원작) | 2.875 | — | — |
| 작법 품질 A(맨프롬프트) | **2.983** | — | — |
| 작법 품질 B(SeqCard) | 2.958 | — | — |
| **품질 격차 (H − 생성)** | **−0.108** CI[−0.581, +0.213] | ≤ 0.5 | 충족 (그러나 무의미) |

**앵커 게이트 3/3 통과** — 채점자가 성실히 읽었음이 확인된 상태에서 나온 값이다.

### 이 실험이 실제로 알려준 것

1. **거의 완전히 들킨다.** 84문항 중 82문항 정답. 판별 불가와 정반대다.
2. ★**작법 점수는 생성물이 오히려 높다**(격차 −0.108, 즉 생성 > 원작). **못 써서 들키는 게 아니다.** 채점자 사유 72건을 분류하면 ⓒ"글이 나쁘다" 15건(20.8%) 대 ⓓ**"너무 정돈돼 있다·군더더기가 없다·설명적이다" 57건(79.2%)**. 들키는 원인은 결함이 아니라 **질서**다.
3. **SeqCard 조건 B가 A보다 나은 바 없다**(2.958 vs 2.983, 판별도 동일하게 들킴). 이 시험에서 설계층의 기여는 **0**이다.

### 신규 결함

- **D-33** 원작에는 있고 생성물에 없는 것이 "잘 쓴 문장"이 아니라 **불규칙성**(무의미한 여담·리듬 붕괴·설명되지 않는 잔여). 현행 척도(d1~d5)는 이것을 재지 못한다.
- **D-34** LLM 채점자는 자기가 재려는 것(작법 품질)을 못 재면 **잴 수 있는 것(표면 질서)을 잰다.** → CT-06 정답표 검증에서 관측된 D-37과 같은 계열.
- **D-35** 작법 품질 격차 ≤0.5 라는 임계는 **판별이 실패했을 때만 의미가 있다.** 판별이 성공한 상태에서 격차가 작다는 것은 성공 증거가 아니다. 사전등록의 AND 조건이 이 함정을 막았다.

산출: `CT-03_2026-08-05_craft_parity_result.md` · 재현 스크립트 `artifacts/CT03_analyze.py` · 요약 `artifacts/CT03_RESULT_summary.json` · 사유 분류 `artifacts/CT03_reason_classification.json` · 문항별 `artifacts/CT03_RESULT_rows.hubsafe.json`.

## 3.6 CT-06 단계 0 — 기능충실 정답표 v1.2 완료 (집행은 대기)

**49요소 / 10씬** (건빵 s7만 4요소, 나머지 5). 난이도 low 8 · mid 21 · high 20. 채점식 `5.0 × matched / n_elements` — 씬별 가변 요소 수를 지원한다. 3차 독립검증 **fail 0 / weak 12 / hold 37**. 사전등록 §9의 집행 차단 조건(정답표 부재)은 **해소**됐다.

**3단 격리 파이프라인이 실제로 잡은 것** — v1.0의 **씬키 오배치**(건빵 `_s15` 블록에 s19 내용, `_s19` 블록에 s15 내용). 그대로 집행했으면 10요소 전부 오판정이었다. 그 외 사실충실 오염 2 · 중복판정 5쌍 · 원문불일치 3.

★**D-37 — LLM 검증자 단방향 표류(이번 라운드의 최대 소득).** 문장이 한 글자도 바뀌지 않은 미개정 요소를 새 검증자가 재판정하면 hold가 weak으로 내려간다. 1차→2차 41요소 일치 0.829, 불일치 7건 **전부 hold→weak, 역방향 0**. 2차→3차 35요소 일치 0.743, 불일치 9건 **전부 hold→weak, 역방향 0**. 두 대조 합쳐 **16/16 단방향**. → **LLM 검증 통과를 품질 증거로 쓸 수 없다.** 라운드를 늘리는 것은 산출 없는 도피다. 이 판단은 3차 검증 **착수 전** 고정한 종료 규칙(`fail 0 AND weak ≤ 5`)으로 사전 보호했다.

**권고 = 안 3** — 집행하되 **요소 종속성을 φ 사후 진단으로 사전등록**한다. "A가 B를 함의하는가"라는 결론 없는 논쟁을 실측 상관계수로 바꾼다. 주 분석 r 은 전 요소로 불변, 고-φ 쌍 병합판을 민감도로 병기. 대안 안1·안2는 상태 보고 §5.

**집행 전 필수 처리** — 난이도 low 8건이 **전부 각 씬 goal**이고 8개 씬에 걸쳐 있다. 전 조건이 자동으로 1.0/5.0 을 얻어 A 조건 바닥이 올라가고 r 의 분모가 압축된다. 하한을 명시하고 r 산출 시 크기를 공시할 것.

산출: `CT-06_2026-08-05_function_key_status.md` · 종료 규칙 `CT-06_2026-08-05_function_key_stopping_rule.md` · 정답표 허브 안전판 `artifacts/CT06_FUNCTION_KEY_v1.2.hubsafe.json` · 검증 3라운드 tally `artifacts/CT06_KEY_VERIFICATION_r{1,2,3}.tally.json`.

## 3.7 ★자기 감사 — 보고서 수치의 재현 (2026-08-05)

같은 세션의 같은 모델이 보고서를 쓰고 그 안의 수치도 냈다. 그래서 **원자료만 읽고 보고값을 다시 계산하는 결정론 스크립트**를 따로 만들어 93개 항목을 대조했다.

| | 대조 항목 | 일치 | 불일치 |
|---|---:|---:|---:|
| CT-03 | 46 | 46 | **0** |
| CT-06 | 47 | 45 | **2** |
| 합계 | **93** | **91** | **2** |

CT-03 은 `analyze.py` 재실행으로 `RESULT_summary.json`·`RESULT_rows.json` 이 **바이트 단위 동일**하게 재생성됐다(시드 20260805 고정).

**불일치 ①** CT-06 상태 보고 §4 의 weak 비율 "0.06 → 0.26 → 0.24" 중 가운데 항이 weak 만이 아니라 `(weak+fail)/50` 이었다. 정정값 **0.060 / 0.240 / 0.245**. 비수렴이라는 §4 의 논지와 판정에는 영향 없음(종료 규칙은 비율이 아닌 건수를 썼다).

**불일치 ②** D-38 의 "r = −0.52 (n=10)" 이 검증 라운드를 명시하지 않았다. 라운드별 재산출 1차 −0.418 / 2차 **−0.516** / 3차(현행 정본) **−0.046**. 현행 정본에서는 상관이 **재현되지 않는다.** n=10 에서 CI 가 0을 포함한다는 사실도 최초 보고에 없었다. → **D-38 을 "2차 기준 관측·현행 미재현"으로 격하.**

★**감사 자신의 한계.** 감사자와 피감사자가 같은 모델·같은 세션이다. 이 감사가 검증한 것은 **원자료와 보고서 사이의 산술 정합**이지 근저 LLM 판단의 타당성이 아니다. CT-03 은 시드 고정으로 완전 결정론이지만, CT-06 의 검증 판정은 LLM 산출이라 재실행하면 달라진다(D-37 이 바로 그 현상이다). 즉 재현된 것은 "저장된 판정 파일에서 보고서의 집계가 도출되는가"이지 "그 판정이 옳은가"가 아니다.

또한 **미검증으로 분류한 주장**이 있다: CT-06 §1 의 "저작자·검증자 모두 금지 파일 미열람을 보고했다"는 **자기 보고**다. 서브에이전트가 실제로 어떤 파일을 열었는지 이 세션은 확인할 수단이 없다. 격리의 실효성은 미검증이다.

재현 방법: `python artifacts/audit_recompute.py --root C:\claude` (원자료만 읽고 보고값과 대조, 출력 `artifacts/AUDIT_OUTPUT.txt`). 전문 `AUDIT_2026-08-05_self_reproduction.md`.

## 4. CTX-01 — 인물 집중도 (탐색적)

CT-02의 렌더 36본 + 원작 12본을 화자 단위로 재분석. **사전등록 없음 → 판정 근거 아님.**

| | 화자 수 | 최다화자 점유 |
|---|---:|---:|
| 원작 (H) | 10.42 | 27.9% |
| A′ 씬층만 | 12.17 | 31.2% |
| B′ +시퀀스층 | 12.83 | **33.1%** |
| N′ 불일치 | 12.08 | 31.8% |

Δ집중도(A′−H) = +3.3%p CI[−1.7, +8.4] · (B′−H) = +5.2%p CI[+0.7, +9.5] · (N′−H) = +3.9%p CI[−0.5, +8.8]
Δ집중도(B′−A′) = +1.9%p CI[−0.0, +3.9] · (B′−N′) = +1.2%p CI[−1.3, +3.6]

**읽는 법.** 생성물은 인물을 **적게 쓰지 않는다. 더 많이 쓴다.** 벌어지는 것은 분배로,
최다화자에게 원작보다 3~5%p 더 몰린다. 그러나 **불일치 대조군 N′도 같은 폭으로 몰린다.**
즉 이것은 생성 전반의 성질이지 시퀀스층 특이적 현상이 **아니다.**
B′−N′ CI가 0을 포함하므로 "시퀀스층이 집중을 악화시킨다"는 주장은 **성립하지 않는다.**

**정정 공시.** 2026-08-05 대화 중 구두로 보고한 값(원작 8.7화자/29%, A′ 10.2/33%, B′ 9.6/36%)은
빈도 절사·병합 규약이 고정되지 않은 임시 산출이었다. 위 표가 확정값이며, 특히 화자 수의
A′↔B′ 대소가 뒤집혔다. 확정 스크립트 = `G2CAST_run_20260805/cast_concentration.py`(시드 20260805).

**측정 한계.** 파서 4규칙 + 빈도 2회 이상 절사 + 끝 2자 병합키(2026-07-28 규약).
과병합·연속행 누락 여지가 있고, 원작 쪽이 과소집계되면 격차가 부풀려진다.
**결정적 범위 제한: 이 실험에서 인물은 씬층을 통해 모델에게 주어졌다.**
따라서 "맨바닥에서 이야기를 구성할 때 인물을 몇 명 세우는가"는 여전히 미측정이다.

## 5. 미완료 큐

| 순위 | 실험 | 선행 조건 | 비고 |
|---|---|---|---|
| ~~1~~ | ~~CT-03 집행~~ | — | **08-05 완료 → FAIL.** §3.5 |
| ~~1'~~ | ~~CT-06 단계 0 정답표 저작~~ | — | **08-05 완료 (v1.2, 49요소, fail 0).** §3.6 |
| **1** | **CT-06 집행 전 개발자 택일** — 안1(요소 축소) / 안2(1차 종점 재검토) / **안3 φ 사후진단 사전등록(권고)** | 없음 | 이것이 지금 유일하게 막힌 선행이다 |
| 2 | **CT-06 집행** — 생성 130매 → 렌더 20본 → **40본 1배치** 채점 | 위 택일 → 사전등록 개정 | `authored_seq/` 유일 유예 조건이자 LLM-2 관문. ~$25–30 |
| 3 | **CT-05** — 회차층 조향, 이중 모델 3팔 | 표본 추출기 | v1.1 등록 완료, 채점기 sha 고정 |
| 4 | **CT-04 (신설 제안)** — `pov_char` 배열 vs 단일값 소규모 대조 | 2~3작 시험 저작 | 전면 재저작 **이전**에 |
| 5 | CTX-02 — 해석 편차 바닥값 | CT-06 렌더 파이프 공유 | 탐색적, ~$8–10 |
| 6 | 장편 일관성 | 척도 미정 | 제품 주장 본체 |

**주의(2026-08-05 정정)**: 구 §5에서 CT-05를 "시퀀스층→씬층 생성"으로 적었던 것은 오기다.
시퀀스층→씬층 생성 시험은 **CT-06**이고, CT-05는 **회차층→시퀀스층 조향**이다.

## 6. 기준선 (이후 개선안이 넘어야 할 값)

씬층 사실충실 **1.24** / 기능충실 **3.20** / 인물 재현율 **0.83**
시퀀스 거시충실 A′ **3.667** (씬층만으로 도달하는 천장)
생성 인물 집중도 **31~33%** vs 원작 **27.9%**

---
## 정정 (2026-08-05, 개발자)
본 트랙 문서들과 08-05 세션 요약에 반복된 **"5년"은 오기다. 실제 프로젝트 기간은 약 5개월.** 각 문서의 "5년" 서술(기회비용·회피 습관·미측정 기간 등)은 전부 "5개월"로 읽는다. 판정·수치에는 영향 없음. 함의는 오히려 반대 방향 — 5개월 만에 코퍼스 95작+엔진 v795+확증 트랙까지 도달한 것이며, "반증 실험 지연"의 심각성도 5년치가 아니라 5개월치다.

---
## 성공의 최종 정의 (개발자 확정, 2026-08-05)
**"원작을 얼마나 되살렸나"가 아니라, 작법의 품질이 인간 작가와 대등 혹은 이상이면 성공이다.**
이에 따라 자(척도)의 위계를 고정한다:
1. **사실충실** = 복원 진단용(원본 있는 시험에서만) — 성공 판정에 사용 금지.
2. **기능충실** = 중간 진단용 — "기계가 설계 사다리를 내려올 수 있는가"(CT-06 등 메커니즘 시험)의 자. 성공 판정이 아니라 누수 지점 지목용.
3. **작법 필적(인간 대비)** = **최종 판정의 자** — CT-03 계열. 블라인드에서 인간 작가의 것과 구별 불가(판별 CI<65%)이고 작법 품질 격차 ≤0.5면 성공. 짜깁기 여부는 묻지 않는다(개발자: "시청자는 창작의 존재론을 묻지 않는다").
단서: LLM 채점자는 매끄러운 문장 편향이 있어 생성물에 유리하게 기운다 — CT-03 계열의 **최종 판정은 인간이 본다**(LLM은 스크리닝만). 이는 아키텍처 정본의 "인간 작가=최종 게이트" 원칙과 일치한다.

---
## CT-03·CT-06H 해석 정정 (개발자, 2026-08-07)
1. **CT-03 판정(FAIL)은 유지하되, 실패가 아니라 "용도 분기"로 읽는다.** 잰 것은 "인간 방송 대본과의 구별 불가능성" 하나다. 작법 점수는 대등 이상(−0.108, 단 LLM 채점 편향 유보)이므로: (a)인간 독자·제작용 트랙 → 불규칙성 재현 필요(D-34 척도 신설 후 착수) (b)**AI 영상 생성기 입력용 비트 대본 트랙 → "정돈됨"은 결함이 아니라 적합 속성, 현재 능력으로 이미 유효 후보** — 제품 전략의 새 갈래로 등재. 작법 모방은 수확 원문 25,500쌍을 교재로 추후 학습 트랙에서 해결(구조 먼저, 문체 나중).
2. **CT-06H 불성립(r=0.21)의 스코프 명시**: 기각된 것은 top-down 구조가 아니라 **현행 5필드 인코딩**이다. 얇은 설계도의 시험이었으므로 낮은 r은 예상 범위 — 가치는 실측화(0.21 기준선)·결손 지목(conflict·info 전멸)·측정 장치 검증(A=N″=0, 일치 95.9%). 본선은 후판(10필드) 재시험이며, **B‴ 천장 팔**(정답표 요소를 설계도로 렌더 — 설계-검사 동형 원칙) 추가로 상한 앵커를 병행 실측한다.

---
## CT-07R — 후판 재현 관문 (2026-08-07 집행, PASS)

보고서 `CT-07R_2026-08-07_result.md` · 방법 `CT-07R_2026-08-07_execution_method.md` · 원자료 `artifacts/ct07r/`

작품 2개(101번째프로포즈·38사기동대), 사전등록 §2 규칙으로 후판 저작 **이전에** 구조 선정된 앵커 10건,
팔 4종(A 무설계 / B 사람 씬카드 / T 정답 후판 / TN 불일치 후판) = 렌더 40본.
격리 병렬 렌더 에이전트 8기, 맹검 채점자 3기(팔 정보 차단, `evidence` 필드 제거).

| 블록 | A | B | T | TN | B−A | r_T | D_N |
|---|---|---|---|---|---|---|---|
| 전체 | 0.167 | 3.267 | 2.700 | 0.167 | 3.100 | **0.817** | **+2.533** |
| 101번째프로포즈 | 0.067 | 3.333 | 2.533 | 0.200 | 3.267 | 0.755 | +2.333 |
| 38사기동대 | 0.267 | 3.200 | 2.867 | 0.133 | 2.933 | 0.886 | +2.733 |

**★이 실험의 가장 중요한 산출물은 전체 r_T 가 아니라 범주 분해(AMD-01 §3.4)다.**

| 부류 | n | B | T | r_T | D_N |
|---|---|---|---|---|---|
| 씬내부 판정 | 32 | 3.639 | 2.153 | **0.567** | +1.944 |
| 배치관계 판정 | 18 | 2.375 | 3.292 | **1.386** | +3.292 |

사후(비사전지정) 5범주 분해 — 정답표가 채점 이전부터 요소마다 달고 있던 원 범주:

| 범주 | n | B | T | r_T | D_N |
|---|---|---|---|---|---|
| character | 10 | 4.500 | 2.667 | 0.542 | +2.333 |
| goal | 10 | 4.000 | 2.500 | 0.625 | +2.500 |
| conflict | 10 | 3.000 | 1.500 | 0.500 | +1.000 |
| info | 10 | 2.667 | 3.667 | **1.429** | +3.667 |
| link | 10 | 2.167 | 3.167 | **1.462** | +3.167 |

후판의 측정된 우위는 `info`·`link` 두 범주에만 있다. `character`·`goal`·`conflict` 는 전부 임계 0.70 아래다.
`D_N`은 다섯 범주 전부 양수이므로 조건 4는 절단면에 무관하게 성립한다.
**주의**: 이것은 필드 해체가 아니다. 다섯 필드가 함께 주어진 상태의 요소별 분해이므로 필드별 기여로 읽으면 안 된다.

전체 0.817은 **두 개의 다른 현상이 섞인 값이다.** 씬 내부에서 무엇이 일어나야 하는지는
사람 씬카드가 여전히 후판보다 낫고(0.567 < 임계 0.70), 앞뒤 씬과의 배치 관계에서는
후판이 씬카드를 넘어선다(1.386 > 1.0). AMD-01 §1이 렌더 이전에 예측한 편향이 실측으로 확정됐다.
**조건 4(TN<T)는 두 부류 모두에서 성립**하므로 분량 교란 가설 기각은 부류에 무관하게 유지된다.

민감도: S2 leave-one-out `r_T` 최소 0.727 / 최대 0.905 → `FRAGILE` 병기 불요.
S1(6행 이상 앵커, n=2) `r_T`=0.481 — AMD-01 §3.2 규정대로 판정에 쓰지 않고 불안정성 지표로만 보고.

**효과 크기는 재현되지 않았다.** CT-07의 후판 직접 렌더 `r`=1.63 대 CT-07R `r_T`=0.817.
부호와 존재는 재현됐고 크기는 절반이다. 절대 수준도 낮다(T 2.700/5 = 54%).

**이 판정이 승인하는 것**: `DB98_THICK_SEQUENCE_EXTENSION_V1` 후판 사이드카 98작 전량 저작.
**승인하지 않는 것**: ①최소 필드 사양(해체 실험 별도 필요 — 98작 저작 시 **필드를 융합하지 말 것**)
②회차→시퀀스 단(한 번도 측정된 적 없음) ③씬 내부 설계의 대체(r_T 0.567, 후판은 씬카드를 대체하지 않고 배치관계 층을 채운다).

**사전등록 이탈**(§8 필수 보고): AMD-02 §4.2 절대 씬 번호 **양팔 대칭 삭제**는 사전등록 §5 문언과 다른 처리다(렌더 0건 시점 사전지정 개정으로 고정). AMD-02 §4.3 쌍별 최소값 절단은 사전등록에 없던 규칙이며 T도 절단하므로 편향은 PASS 반대쪽. **임계는 하나도 변경되지 않았다.**

**미해결 — 규칙 분기.** 허브 선행 문서 `LOS-CT07R-PREREG-AMENDMENT-02`(GPT)는 정보량 균등화를
**TN 채워 올림** 방향으로 규정했고, 집행분 `LOS-CT07R-AMD-02`(본 세션)는 **양팔 절단** 방향이다.
집행은 후자를 썼다 — `r_T` 에 대해 더 가혹한 쪽이다. 전자 규칙 아래의 판정은 새 렌더 세트가 있어야 답한다.
상세 대조는 `CT-07R_2026-08-07_amendment_02_as_executed_normalization.md` §0.5.

---

## 7. CT-08A — 후판 5필드 절제 파일럿 (2026-08-08 집행, **무효**)

상세: `CT-08A_2026-08-08_ablation_pilot_prereg.md` · `CT-08A_2026-08-08_result.md`
산출물: `artifacts/ct08a/` (509파일) · 재현 `python3 CT08A_analyze.py`

5팔 × 24앵커 = 120본. B(사람 씬카드) / T5(후판 5필드) / T-F(기능축만) / T-I(정보축만) / TN(중립).
정답표 240요소, 3인 독립 블라인드 채점.

| 팔 | 기능충실 (요소 만장일치 다수결) |
|---|---:|
| B | 3.292 |
| T5 | 3.188 |
| T-F | 2.708 |
| T-I | 1.708 |
| TN | 0.042 |

| 대비 | Δ | 95% CI | McNemar |
|---|---:|---|---|
| Δ_true = T5 − T-I | **+1.479** | [+0.958, +2.000] | b=83 c=12 p=3.21e-14 |
| Δ_FI = T-F − T-I | +1.000 | [+0.521, +1.479] | b=63 c=15 p=3.75e-08 |
| Δ_app = T5 − T-F | +0.479 | [+0.104, +0.896] | b=42 c=19 p=0.00444 |
| B − T5 (교락 점검) | +0.104 | [−0.271, +0.479] | b=32 c=27 p=0.603 |

**이 수치를 채택하지 않는다.** 사전등록 §8-2 무효조건 2(쌍별 요소 일치도 ≥0.90)가
S1-S2 0.8783 · S1-S3 0.8525 · S2-S3 0.8942 로 **3쌍 전부 미달**했다.
일치도 0.90 은 집행 전에 확정된 기준이고, 결과를 본 뒤 낮추지 않는다.

살아남는 것은 **방향에 대한 관측**뿐이다: 정보축 제거가 기능축 제거보다 타격이 크고
(Δ_FI > 0), 5필드 전량이 단일 축보다 낫다(Δ_app > 0). D-52 재기술 교락은 실재하는
방향으로 관측됐으나 확정하지 않는다.

**신규 결함 D-53 · D-54** → `docs/standards/CT_DEFECT_REGISTER_2026-08-08.md`

- **D-53** 기능충실 요소 채점의 재현성 부족. 불일치는 팔의 함수가 아니라 경계 요소의 함수
  (TN 0.967 → T5 0.817).
- **D-54** 이진 일치도의 기저율 팽창. 비자명 요소(최소 1인이 1)로 제한하면
  CT-08A 0.8750 → **0.7660**, **CT-07R 0.9533 → 0.8621**.
  즉 CT-07R 이 근거로 쓴 "3인 일치 95.3%"는 기저율이 끌어올린 값이고,
  실제 판정이 갈리는 요소에서는 CT-08A 의 무효 기준 0.90 을 **CT-07R 도 넘지 못한다.**

**CT-07R 판정 처리** — 철회하지 않는다. CT-07R 은 자기 사전등록의 무효조건을 위반하지 않았다
(그 사전등록에 비자명 일치도 조항이 없었다). 다만 지위를 **"계측기가 검증되지 않은 상태의 PASS"**
로 강등하고, **이 PASS 를 근거로 후판 98작 전량 저작을 착수하지 않는다.**

**다음에 해야 할 일 (순서 고정)**

1. 채점 규칙 개정 — 요소 문법 정규화 + 조정자 단계. **관측 수치를 보기 전에** 정당화한다.
2. 개정 규칙으로 CT-08A 재채점. 동시에 CT-01·CT-06H·CT-07 을 비자명 일치도로 재산출.
3. 그 다음에야 절제 실험(잔여 4필드)과 98작 전량 저작 판단.

계측기를 고치기 전의 모든 대결 실험은 또 무효가 될 수 있다.

---

## 8. 병렬 트랙 — 스키마 소비 계약 (2026-08-08)

`docs/design/2026-08-08_SCHEMA-RATIONALE-AND-CONSUMPTION-v1.md`

확증 트랙이 계측기 수리로 막힌 동안 진행 가능한 별개 전선이다.
2달간 구축한 분석층이 왜 소비되지 않는지를 전수 조사한 결과, 원인은 **배선 누락이 아니라
소비 계약 미협상**이었다. LLM-2 본선 설계도 11종이 분석층을 입력으로 선언한 사례 **0건**.
검색 표적화는 데이터상 성립하며(7필드 100% 충전·저카디널리티), 진짜 병목은 **커버리지**다
(chroma 가 정본과 교집합 1작인 다른 코퍼스를 본다 · 메타데이터 키 0종 · cast 35/98작).

이 트랙은 **공정이지 효용 증거가 아니다**. 배선을 완성해도 효용은 계측기가 고쳐진 뒤에야 잰다.
